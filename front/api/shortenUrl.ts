export async function shortenUrl(text: string) {
  try {
    //const response = await fetch("/shorten_url", { method: "POST", body: JSON.stringify({ text }) });
  } catch (e) {
    return { error: "An error has occurred" };
  }
}
